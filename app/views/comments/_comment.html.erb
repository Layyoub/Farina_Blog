<% unless comment.archived? %>
  <div class="bg-black rounded-lg shadow-lg p-4 mb-4 text-white">
    <p class="text-gray-300">
      <strong class="text-yellow-400">Commenter:</strong>
      <%= comment.commenter %>
    </p>

    <p class="text-gray-300">
      <strong class="text-yellow-400">Comment:</strong>
      <%= comment.body %>
    </p>

    <p>
     <div class="flex space-x-4 mb-8">
    <%= link_to "Wingardyum Levi'oosaa", edit_article_path(@article), data: { turbo_frame: "edit_article" }, class: "bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded" %>
    <%= link_to "AVADA KADABRA!", article_path(@article), method: :delete, data: { turbo_confirm: "Are you sure?", turbo_frame: "articles" }, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
    </p>
  </div>
<% end %>
